# Traefik

This directory represents the ingress and reverse-proxy layer used in the homelab.

The real configuration is intentionally not public because routing rules, upstream mappings, certificate wiring, and service-integration details can reveal too much about the live environment.
