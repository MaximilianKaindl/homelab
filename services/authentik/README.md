# authentik

Identity provider and authentication service for the homelab platform.

The public repo includes the core service payload while leaving out environment-specific ingress, public hostname, and edge-auth integration details.
