# Services

This directory represents the application and platform-service layer of the homelab.

The public repository excludes the real service roots because those projects contain deployment topology, routing assumptions, environment-specific wiring, and other details that are not safe to publish as-is.
