# Services

This directory represents the application and platform-service layer of the homelab.

The public repository includes a curated subset of real service payloads with environment-specific ingress, DNS, and platform-wiring details removed or parameterized.

Service roots that still expose edge routing, firewall policy, public DNS, internal registry endpoints, or control-plane specifics remain excluded.
