# Bootstrap

This directory represents the machine-bootstrap layer of the homelab.

The public repository intentionally does not include the real bootstrap projects because they contain environment-specific machine identities, provisioning details, and operational assumptions.
