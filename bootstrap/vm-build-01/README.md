# vm-build-01

This machine acts as a build-oriented host used for automation and delivery tasks.

The public repo intentionally omits the actual provisioning, CI, and machine-specific configuration so internal infrastructure details do not leak.
