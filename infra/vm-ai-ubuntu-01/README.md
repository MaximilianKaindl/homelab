# vm-ai-ubuntu-01

This virtual machine hosts AI-oriented workloads and supporting runtime components.

The published repo keeps this at the documentation level only and leaves the real provisioning logic private.
