# Infrastructure

This directory represents infrastructure environment roots for the homelab.

The public repository includes selected VM roots that are still useful after removing internal Git host references and other environment-specific wiring.

Infrastructure projects tied to DNS, firewall policy, vendor control planes, edge exposure, or live topology remain excluded.
